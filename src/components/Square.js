import React from 'react';
import ReactDOM from 'react-dom';

const Square = ({ coordinate }) => (
    <div
        className={'square'}
        id={coordinate}
    >
    </div>
);

export default Square;
