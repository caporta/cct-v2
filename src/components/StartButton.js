import React from 'react';


const StartButton = () => (
    <div>
        <button
            className={'button button__start'}
        >
            Start
        </button>
    </div>
);

export default StartButton;
