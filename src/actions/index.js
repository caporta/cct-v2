export const toggleSide = (side) => ({
    type: 'TOGGLE_SIDE',
    side
});

export const incrementScore = () => ({
    type: 'INCREMENT_SCORE'
});

export const toggleGame = () => ({
    type: 'TOGGLE_GAME'
});
